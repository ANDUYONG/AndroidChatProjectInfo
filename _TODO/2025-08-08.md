___

- [ ] 1. 채팅방 생성
    - [ ] 1.1. 채팅방 생성
    - [ ] 1.2. `chattingRooms`에 문서 1개 생성
        - [ ] 식별값 필드에 추가 > `uid`
        - [ ] `users` 필드에 대상 유저 추가
    - [ ] 1.3. `users` > `chattingRooms` | `chattings` `uid` 와 함께 문서 추가
        - [ ] `chattings`에 문서를 `trigger`
        - [ ] `users` 필드를 순회하며 `userId`를 가지고 해당 user에 `uid`를 추가
        - [ ] `chattings`에 `type = "in"` 인 메시지 저장 "OOO님이 채팅방에 OOO님을 초대하셨습니다."
- [ ] 2. 첫 채팅 메시지 보내기
    - [ ] 2.1. 채팅방 화면에 있으면 `chattingRooms`의 `onUsers`에 로그인 한 사용자 `userId`를 추가
        - [ ] 채팅방 화면에서 벗어나면 `onUsers`에서 본인의 `id`를 제거
    - [ ] 2.2. 메시지 추가
        - [ ] 메시지 시퀀스(Sequence) 추가
            - [ ] 트랜잭션(transaction)
        - [ ] 추가 시 `onUsers`에 존재하지 않는 `userId`를 `unreadUsers`에 추가 후 저장
            - [ ] `isExist = true`
            - [ ] `onUsers`에 존재하지 않는 `userId`
        - [ ] `chattingGroups`의 `chattings`를 업데이트
            - [ ] `trigger` > `chattingRooms`의
                - [ ] `lstMsg`, `lstProfileUrl`, `lstSendDate` 업데이트
            - [ ] `trigger` > `chattingRooms` > `users`의
                - [ ] `unreadCnt` 업데이트