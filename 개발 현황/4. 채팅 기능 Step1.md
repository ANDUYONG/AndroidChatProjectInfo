___
- [x] 1. 채팅방 생성
    - [x] 1.1. 채팅방 생성
    - [x] 1.2. `chattingRooms`에 문서 1개 생성
        - [x] 식별값 필드에 추가 > `uid`
        - [x] `users` 필드에 대상 유저 추가
    - [x] 1.3. `users` > `chattingRooms` | `chattings` `uid` 와 함께 문서 추가
        - [x] `chattings`에 문서를 `trigger`
        - [x] `users` 필드를 순회하며 `userId`를 가지고 해당 user에 `uid`를 추가
        - [x] `chattings`에 `type = "in"` 인 메시지 저장 "OOO님이 채팅방에 OOO님을 초대하셨습니다."
- [x] 2. 첫 채팅 메시지 보내기
    - [x] 2.1. 채팅방 화면에 있으면 `chattingRooms`의 `onUsers`에 로그인 한 사용자 `userId`를 추가
        - [x] 채팅방 화면에서 벗어나면 `onUsers`에서 본인의 `id`를 제거
    - [x] 2.2. 메시지 추가
        - [ ] 메시지 시퀀스(Sequence) 추가
            - [ ] 트랜잭션(transaction)
        - [x] 추가 시 `onUsers`에 존재하지 않는 `userId`를 `unreadUsers`에 추가 후 저장
            - [x] `isExist = true`
            - [x] `onUsers`에 존재하지 않는 `userId`
        - [x] `chattingGroups`의 `chattings`를 업데이트
            - [x] `trigger` > `chattingRooms`의
                - [x] `lstMsg`, `lstProfileUrl`, `lstSendDate` 업데이트
            - [x] `trigger` > `chattingRooms` > `users`의
                - [x] `unreadCnt` 업데이트
- [x] 3. 채팅방 목록 조회
	- [x] 컬렉션 변경 감지 리스너 설정
	- [x] 안 읽은 메시지 개수 (`users` > `userId` > `unreadCnt`) 
	- [ ] 가장 최근 메시지 (10자 이내로 보여주기...) 
	- [x] `profileUrl` 이미지 로딩
- [x] 4. 채팅방 메시지 목록 조회
	- [x] 변경 감지 리스너 설정 
	- [x] 추가된 메시지 가져와서 추가
	- [ ] 최근 10개 항목 조회 
	- [ ] 스크롤 페이징 설정
- [x] 5. 채팅방 알림 끄기
    - [ ] 채팅방 정보 조회
        - [ ] 해당 유저의 알림 `off`
        - [x] 시스템 푸시(push) 컨트롤
- [ ] 6. 채팅방 친구 추가 하기
    - [ ] `chattingRooms` > `users`에 사용자 추가
    - [ ] `users` > `chattingRooms` `uid` 추가
- [ ] 7. 채팅방 나가기
    - [x] `users` > `chattingRooms` `uid` 제거
    - [x] `chattingRooms`의 해당 `userId`의 `isExist` > `false`로 변경
    - [ ] `chattings`에 `type = "out"` 인 메시지 저장 "OOO님이 채팅방에서 나가셨습니다."